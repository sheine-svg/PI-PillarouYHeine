import React from "react";

function Login(){
    return(
        <div>Login</div>

        /*
        let recuperoStoragePassword = localStorage.getItem('password');
        let passwordsRecuperados = JSON.parse(recuperoStoragePassword);
        */
    )
}

export default Login;